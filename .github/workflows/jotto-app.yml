name: jotto-appl

on:
  push:
    branches:
      - master
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: install NPM packages
        run: npm install

      - name: run tests
        run: npm run test
